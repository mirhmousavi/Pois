language: python

install:
        - pip install -r requirements.txt

jobs:
        include:
                - stage: test code
                  python: "3.5"
                  script: ls
                - stage: more
                  script: kkkk
                - stage: deploy code
                  python: "3.5"
                  install: python -V
                  script: echo date
                  after_success: echo 'redondo'

